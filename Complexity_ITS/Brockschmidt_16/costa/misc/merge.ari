(format LCTRS)
(theory Ints)
(fun merge (-> Int Int Int))
(fun start (-> Int Int Int))
(entrypoint start)
(rule (merge A B) (merge (- A 1) B) :guard (and (>= A 1) (>= B 1)))
(rule (merge A B) (merge A (- B 1)) :guard (and (>= A 1) (>= B 1)))
(rule (start A B) (merge A B))
